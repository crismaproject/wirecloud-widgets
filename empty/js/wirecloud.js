if (typeof MashupPlatform === 'undefined') {
    // we are not within Wirecloud, the API wasn't injected! Someone probably opened your HTML file directly.
    console.warn('The Wirecloud environment was not detected.');
} else {
    // wirings, preferences, etc.
}